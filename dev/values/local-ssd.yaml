storageClass:
  create: true
  defaultClass: false
  name: ssd
  reclaimPolicy: Delete

nodePathMap:
  - node: DEFAULT_PATH_FOR_NON_LISTED_NODES
    paths:
      - /mnt/ssd

configmap:
  name: local-ssd-config
